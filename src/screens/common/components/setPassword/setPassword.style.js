import { StyleSheet } from 'react-native';

const componentStyle = StyleSheet.create({
  contentContainer: {
    alignItems: 'flex-end',
  },
  passwordComponentStyle: {
    top: 4,
  },
});

export { componentStyle };
