import { StyleSheet } from 'react-native';

const componentStyle = StyleSheet.create({
  titleContainer: {
    paddingVertical: 8,
  },
  titleText: {
    textAlign: 'center',
  },
});

export { componentStyle };
