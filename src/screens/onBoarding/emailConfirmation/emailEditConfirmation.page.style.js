import { StyleSheet } from 'react-native';

const pageStyle = StyleSheet.create({
  emailConfirmationContainerStyle: {
    padding: 32,
  },
  oldEmailStyle: {
    marginBottom: 24,
  },
});

export { pageStyle };
