import { StyleSheet } from 'react-native';

const pageStyle = StyleSheet.create({
  mainContainer: {
    flex: 1,
    paddingHorizontal: 32,
  },
  buttonsContainer: {
    alignItems: 'center',
    padding: 32,
  },
});

export { pageStyle };
