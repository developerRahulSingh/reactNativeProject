import { StyleSheet } from 'react-native';
import commonTheme from '../../../themes/common.theme';

const pageStyle = StyleSheet.create({
  pageColorStyle: {
    backgroundColor: commonTheme.COLOR_PRIMARY,
  },
  renderFieldContainerStyle: {
    paddingHorizontal: 32,
  },
});

export { pageStyle };
