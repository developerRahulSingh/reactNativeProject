import { connect } from 'react-redux';
import SelectSellAssetsOptionPage from './selectSellAssetsOption.page';

const mapStateToProps = state => {
  return {};
};

const mapDispatchToProps = dispatch => {
  return {};
};

export default connect(mapStateToProps, mapDispatchToProps)(SelectSellAssetsOptionPage);
