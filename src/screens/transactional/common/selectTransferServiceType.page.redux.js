import { connect } from 'react-redux';
import SelectTransferServiceTypePage from './selectTransferServiceType.page';

const mapStateToProps = state => {
  return {};
};

const mapDispatchToProps = dispatch => {
  return {};
};

export default connect(mapStateToProps, mapDispatchToProps)(SelectTransferServiceTypePage);


