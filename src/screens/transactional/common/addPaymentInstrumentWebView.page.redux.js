import { connect } from 'react-redux';
import AddPaymentInstrumentWebViewPage from './addPaymentInstrumentWebView.page';

const mapStateToProps = state => {
  return {};
};

const mapDispatchToProps = dispatch => {
  return {};
};

export default connect(mapStateToProps, mapDispatchToProps)(AddPaymentInstrumentWebViewPage);
