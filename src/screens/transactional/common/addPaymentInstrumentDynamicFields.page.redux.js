import { connect } from 'react-redux';
import AddPaymentInstrumentDynamicFieldsPage from './addPaymentInstrumentDynamicFields.page';

const mapStateToProps = state => {
  return {};
};

const mapDispatchToProps = dispatch => {
  return {};
};

export default connect(mapStateToProps, mapDispatchToProps)(AddPaymentInstrumentDynamicFieldsPage);
