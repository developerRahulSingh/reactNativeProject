import { connect } from 'react-redux';
import SelectPaymentInstrumentTypePage from './selectPaymentInstrumentType.page';

const mapStateToProps = state => {
  return {};
};

const mapDispatchToProps = dispatch => {
  return {};
};

export default connect(mapStateToProps, mapDispatchToProps)(SelectPaymentInstrumentTypePage);
