import { connect } from 'react-redux';
import SelectPaymentInstrumentPage from './selectPaymentInstrument.page';

const mapStateToProps = state => {
  return {};
};

const mapDispatchToProps = dispatch => {
  return {};
};

export default connect(mapStateToProps, mapDispatchToProps)(SelectPaymentInstrumentPage);


